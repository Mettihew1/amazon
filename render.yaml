cat << 'EOF' > render.yaml
static_sites:
  - name: eesy-ir  # Replace with your site name
    build_command: npm run build
    publish: dist/  # Or "build/" if your Vite uses that
    routes:
      - path: "/*"
        rewrite: "/index.html"
EOF